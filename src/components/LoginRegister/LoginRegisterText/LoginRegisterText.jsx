import {LogRegWrapper} from './LoginRegisterText.styled'

export function LoginRegisterText() {
    return (
      <LogRegWrapper>
        Or log in using an email and password, after registering:
      </LogRegWrapper>
    );
}